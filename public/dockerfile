# Step 1: Use an official Nginx image as a base
FROM nginx:alpine

# Step 2: Copy your website files into the Nginx serving directory
COPY . /usr/share/nginx/html

# Step 3: Expose port 80 to the outside world
EXPOSE 80